# Calculateur de distances cosmologiques — dépendances
#
#   pip install -r requirements.txt
#
# scipy n'est PAS optionnel : astropy.cosmology s'en sert pour les quadratures
# (comoving_distance, age, lookback_time).

numpy>=1.24
scipy>=1.10
astropy>=6.0
PyQt6>=6.5
pyqtgraph>=0.13
